# 进度记录

## 已完成

- 已完整阅读实际存在的总规范 `commercial_center_v1/ARTDON_COMMERCIAL_CENTER_MASTER_SPEC.md`（V1.1，共 1924 行）；用户指定的 `docs/commercial-center/...` 路径在当前仓库不存在。
- M0 已完成并通过：安全审计、旁路目录、10 个只读适配器、迁移/回滚 dry-run、安全文档和独立健康检查。
- M1 已完成并通过：商务中心外壳、可折叠菜单、三大核心入口、紧凑状态栏、本人范围工作队列、商务交付队列、异常提醒、订单进度与近期动态。
- M2 已完成只读基础里程碑：产品和物料页面、实际旧权限控制、搜索、分类筛选、详情抽屉；未建立正式配置规则、编辑或保存功能。
- 核验真实 Git 项目根、Nginx Web 根、域名、Git 状态、PHP/MySQL 和实际数据库。
- 审计统一登录、Session、权限读取、公共布局、资源与主要业务入口。
- 审计商务中心相关旧表的实际名称、主键和重要关联字段。
- 建立独立目录、最小安全入口、健康检查、独立资源和独立存储目录。
- 建立 10 个只读 Legacy 适配器及认证/权限契约。
- 建立并执行 4 张 `cc_*` 基础表的迁移，保留回滚方案、dry-run 和 schema manifest。
- 新增仅限 CLI、必须显式传入 `--apply` 且只接受白名单 `CREATE cc_*` 语句的迁移执行器。
- 创建全部安全文档与测试脚本。

## 正在进行

- 无。基础建表、验证、文档更新和部署均已完成。

## 待确认

- 下一阶段正式产品扩展、配置组和兼容规则的数据模型仍须独立评审。
- 已登录浏览器会话下的人工视觉验收；当前自动化环境没有可用浏览器实例。

## 发现的问题与风险

- 实际数据库名是 `artdon_new_erp`，用户已确认按实际值继续。
- 本机无 PHP CLI，需在服务器检查。
- 旧报价代码含自动补 schema 行为，新模块禁止调用。
- 邮件正文、附件、客户信息和成本数据敏感，V1 只做最小状态检测。
- 总规范原文中的数据库名与预计路径不是实际环境值，实际值以安全基线为准。
- 总规范第 36 节范围大于最初 M0 指令；用户后续明确授权连续完成 M1，并在时间允许时继续 M2。

## 创建文件

所有文件均在 `commercial_center_v1/`；完整清单以 `find commercial_center_v1 -type f` 和 Git 新增文件为准。

## 数据库变化

- 迁移执行：是，经用户于 2026-07-24 明确批准。
- 新表：4
- 已建表：`cc_schema_migrations`、`cc_entity_links`、`cc_integration_logs`、`cc_activity_logs`
- 表引擎：InnoDB；字符集/排序规则：`utf8mb4` / `utf8mb4_general_ci`
- 旧表结构、旧索引、旧触发器及旧业务数据变化：0

## 测试结果

- 全部新模块 PHP 文件通过服务器 `php -l`。
- `tests/safety_scan.php`：通过。
- `tests/bootstrap_smoke.php`：通过。
- `tests/operations_smoke.php`：通过，实际旧用户范围只读查询。
- `tests/catalog_smoke.php`：通过，实际产品与物料只读查询。
- 正式迁移：执行器白名单校验通过，4 张表逐表存在性验证通过。
- 迁移前后旧系统基线一致：240 张旧表、4079 个旧字段、1157 条旧索引记录、0 个旧触发器。
- 独立首页与健康接口：HTTP 200。
- 自动化浏览器：当前没有可用浏览器实例，未进行截图级视觉验收。

## 旧系统变化

- 旧文件修改：0
- 旧文件删除：0
- 旧文件重命名：0
- 旧表结构变化：0
- 旧业务数据变化：0

## 下一阶段准确入口

独立 URL：`https://novlight.com/artdon_erp/commercial_center_v1/`。下一阶段从 M2 正式数据模型评审开始；任何新增 `cc_*` 业务表仍须单独评审，不修改旧菜单。
