# 进度记录

## 2026-07-25 配置引擎与极简配置器

- [x] 暂停继续扩展 PDF、邮件和转订单；M4 报价入口改为先进入配置器。
- [x] 建立 12 张配置引擎 `cc_*` 表：模板、版本、组设置、模板组、产品模板、允许选项、预设、预设值、锁定规则、物料映射、配置实例及配置快照。
- [x] 建立 16 类通用配置组；单选、多选、数字、文本、必选、高级、可见性及成本/价格/MOQ/交期影响均由数据驱动，不为品类复制页面代码。
- [x] 建立工厂标准、经济、标准、高配及新加坡渠道预设；个人/客户预设通过登录与 CSRF 仅写 `cc_*`。
- [x] 服务层完成库存 SKU 硬锁、产品允许选项、禁止/警告/审批兼容规则、配置护照、差异项、成本售价、MOQ 与交期计算。
- [x] 配置选项与 `cc_materials` 分离，并建立默认/替代物料映射。
- [x] 极简配置器完成快速/专业/定制、关键/高级折叠、A/B/C、恢复标准、客户预设、复制、保存预设和加入报价。
- [x] “加入报价”在同一事务链创建配置实例、不可变快照、报价版本、明细和明细快照；报价预览读取相同快照。
- [x] 服务器 PHP 全量语法、配置引擎回滚烟测、安全扫描及 JavaScript 解析检查通过。
- [ ] 已登录真实浏览器视觉与交互验收：自动化环境没有可用浏览器实例，保留人工复核。

## 2026-07-25 商务中心正式底座补齐

- [x] 建立统一 `config/menu.php`，注册八大分组及全部页面入口。
- [x] 现有入口统一显示标题、面包屑、工具栏、搜索区、主内容空状态和登录/权限状态。
- [x] 保留既有产品、物料、库存、订单、单证和配置器实现；未删除或覆盖旧实现。
- [x] 增加 `007_commercial_foundation.sql` 与对应回滚文件；仅允许创建 `cc_*` 隔离表。
- [x] 补齐商务角色、操作权限和审计落点配置；正式业务动作仍未启用。
- [ ] 服务器 CLI 执行 `007_commercial_foundation.sql`（需按安全基线先快照并由授权人员执行）。

## 已完成

- 已完整阅读实际存在的总规范 `commercial_center_v1/ARTDON_COMMERCIAL_CENTER_MASTER_SPEC.md`（V1.1，共 1924 行）；用户指定的 `docs/commercial-center/...` 路径在当前仓库不存在。
- M0 已完成并通过：安全审计、旁路目录、10 个只读适配器、迁移/回滚 dry-run、安全文档和独立健康检查。
- M1 已完成并通过：商务中心外壳、可折叠菜单、三大核心入口、紧凑状态栏、本人范围工作队列、商务交付队列、异常提醒、订单进度与近期动态。
- M2 已完成只读基础里程碑：产品和物料页面、实际旧权限控制、搜索、分类筛选、详情抽屉；未建立正式配置规则、编辑或保存功能。
- 核验真实 Git 项目根、Nginx Web 根、域名、Git 状态、PHP/MySQL 和实际数据库。
- 审计统一登录、Session、权限读取、公共布局、资源与主要业务入口。
- 审计商务中心相关旧表的实际名称、主键和重要关联字段。
- 建立独立目录、最小安全入口、健康检查、独立资源和独立存储目录。
- 建立 10 个只读 Legacy 适配器及认证/权限契约。
- 建立并执行 4 张 `cc_*` 基础表的迁移，保留回滚方案、dry-run 和 schema manifest。
- 新增仅限 CLI、必须显式传入 `--apply` 且只接受白名单 `CREATE cc_*` 语句的迁移执行器。
- 创建全部安全文档与测试脚本。

## 正在进行

- 夜间连续执行清单（严格顺序）：
  - [x] 安全复核与本轮数据库基线
  - [x] M1-R 首页专业化修正、测试、文档
  - [x] M1-O 统一订单与新加坡订单基础、测试、文档
  - [x] M1-D 四套正式单据旁路复制、测试、文档
  - [x] M2 产品、物料与配置中心基础、测试、文档
  - [x] M3 库存 SKU 与渠道发布基础、测试、文档
  - [x] M4 标准报价基础版与单据预览联调
  - [x] 整体回归、部署、Git 与上下文记录

## 本轮里程碑记录

### M1-R（已完成）

- 首页主内容取消固定宽度，使用 18px 页面边距，并为 1920px 设置合理上限。
- 导航按工作台、产品与报价、订单与交付、系统分组；支持组折叠、桌面折叠记忆和小屏抽屉。
- 技术版本、数据库及适配器状态移至管理员系统状态页；普通首页不再展示 Git/表名/适配器数量。
- 三个核心入口、九项紧凑业务状态、统一商务任务筛选、右侧异常/交付/渠道区完成。
- 旧派工仅做参数化只读读取，非商务任务不进入首页；新加坡渠道明确显示 `not_configured`。
- PHP 语法、安全扫描、bootstrap、运营查询及首页/系统状态 HTTP 测试通过。
- JavaScript 已人工静态检查；本机和服务器均未安装 Node.js，无法运行 `node --check`，已记录为环境限制。

### M1-O（已完成）

- 建立统一订单、订单明细、状态历史、外部订单和外部事件 5 张 `cc_*` 表。
- 广州订单号、外部订单号、幂等键、负载哈希、同步状态/次数/错误分别保存。
- 建立统一订单服务与订单中心页面；当前无正式订单时显示真实空状态。
- `SingaporeChannelAdapter` 固定返回 `not_configured`，未连接真实 API、未伪造同步成功。
- 迁移可重复执行，回滚只包含本阶段 5 张新表；当前 5 张表数据均为空。
- M1-O PHP、安全扫描、bootstrap、订单页面 HTTP 及迁移前后旧库基线检查通过。

### M1-D / M2 / M3 / M4（已完成基础垂直切片）

- M1-D：真实源文件审计与哈希、统一模板注册/渲染、五个 `legacy_v1` 预览入口完成；打印/浏览器 PDF 可用，Excel 明确待接入。
- M2：产品扩展、统一物料、配置组、配置选项和兼容规则 5 表已建；旧命名/BOM继续只读。
- M3：库存 SKU、预占、渠道、销售套餐和发布任务 5 表已建；真实新加坡发布保持 `not_configured`。
- M4：报价、版本、明细和明细快照 4 表已建；登录用户可通过 CSRF 保护只向 `cc_*` 保存草稿，客户预览使用 `legacy_v1`。
- 51 个 PHP 文件、9 个主路由、5 个单据预览、安全扫描和旧系统只读查询回归通过。

## 待确认

- 下一阶段正式产品扩展、配置组和兼容规则的数据模型仍须独立评审。
- 已登录浏览器会话下的人工视觉验收；当前自动化环境没有可用浏览器实例。

## 发现的问题与风险

- 实际数据库名是 `artdon_new_erp`，用户已确认按实际值继续。
- 本机无 PHP CLI，需在服务器检查。
- 旧报价代码含自动补 schema 行为，新模块禁止调用。
- 邮件正文、附件、客户信息和成本数据敏感，V1 只做最小状态检测。
- 总规范原文中的数据库名与预计路径不是实际环境值，实际值以安全基线为准。
- 总规范第 36 节范围大于最初 M0 指令；用户后续明确授权连续完成 M1，并在时间允许时继续 M2。

## 创建文件

所有文件均在 `commercial_center_v1/`；完整清单以 `find commercial_center_v1 -type f` 和 Git 新增文件为准。

## 数据库变化

- 迁移执行：是，经用户于 2026-07-24 明确批准。
- 新表：35（原有23，配置引擎新增12）。
- 表引擎：InnoDB；字符集/排序规则：`utf8mb4` / `utf8mb4_general_ci`
- 旧表结构、旧索引、旧触发器及旧业务数据变化：0

## 测试结果

- 全部新模块 PHP 文件通过服务器 `php -l`。
- `tests/safety_scan.php`：通过。
- `tests/bootstrap_smoke.php`：通过。
- `tests/operations_smoke.php`：通过，实际旧用户范围只读查询。
- `tests/catalog_smoke.php`：通过，实际产品与物料只读查询。
- 正式迁移：执行器白名单校验通过，4 张表逐表存在性验证通过。
- 迁移前后旧系统基线一致：240 张旧表、4079 个旧字段、1157 条旧索引记录、0 个旧触发器。
- 独立首页与健康接口：HTTP 200。
- 自动化浏览器：当前没有可用浏览器实例，未进行截图级视觉验收。

## 旧系统变化

- 旧文件修改：0
- 旧文件删除：0
- 旧文件重命名：0
- 旧表结构变化：0
- 旧业务数据变化：0

## 下一阶段准确入口

独立 URL：`https://novlight.com/artdon_erp/commercial_center_v1/`。下一阶段深化多行报价编辑、Excel独立输出和正式脱敏快照像素级比对；不修改旧菜单。

## 2026-07-25 正式底座入口补齐

- [x] `config/menu.php` 注册八大分组及全部商务中心入口。
- [x] 入口统一提供标题、面包屑、工具栏、搜索区、权限状态和空状态。
- [x] 增加隔离迁移 `007_commercial_foundation.sql` 及对应回滚文件，仅创建 `cc_*` 表。
- [x] 补齐角色、操作权限和审计落点配置；正式业务写入仍未启用。
- [ ] 007 迁移需在服务器完成快照后通过 CLI 执行。

## 2026-07-25 正式底座入口补齐

- [x] `config/menu.php` 注册八大分组及全部商务中心入口。
- [x] 入口统一提供标题、面包屑、工具栏、搜索区、权限状态和空状态。
- [x] 增加隔离迁移 `007_commercial_foundation.sql` 及对应回滚文件，仅创建 `cc_*` 表。
- [x] 补齐角色、操作权限和审计落点配置；正式业务写入仍未启用。
- [ ] 007 迁移需在服务器完成快照后通过 CLI 执行。
