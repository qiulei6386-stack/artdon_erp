# Codex 十阶段连续执行日志

## 第 1 阶段：审计、备份、基线锁定

- 状态：完成。
- 完整阅读 `ARTDON_MATERIAL_CENTER_CODEX_LONG_RUN_MASTER_SPEC_V1.md`（748 行）。
- 锁定 `components/sidebar.php` 为菜单基线，不修改现有菜单。
- 建立本地 Git bundle、服务器目录 tar、全数据库逐表 PDO 备份并完成校验。
- 确认旧 BOM 来源为 `bom_materials`，后续只读。
- 发现外壳与原服务层引导断裂、首页静态数据、业务占位页和旧测试断言四类缺口。
- 修复策略：保留当前外壳，恢复自动加载、统一账号和数据库引导；不恢复旧视觉外壳。

## 阶段记录规则

每阶段记录迁移、服务、接口、页面、权限、测试、旧表只读核对和遗留问题。测试失败必须先修复再进入下一阶段；非阻断问题写入 `KNOWN_ISSUES.md`。

## 第 2 阶段：基础架构与数据库（基础提交）

- 恢复当前外壳缺失的命名空间自动加载、广州 ERP `includes/bootstrap.php`、数据库、Session 和 CSRF 引导。
- 保留现有 `components/` 外壳与左侧菜单；仅让旧服务层和当前外壳能够同时运行。
- 新增可回滚迁移 `20260726_005_master_data_full_domain`，所有新表均使用 `mc_`。
- 覆盖七类扩展、字段注册、审批、操作/修改日志、BOM 来源、导入错误、供应商联系人/阶梯价/MOQ/交期、产品适配、替代引用迁移、文档版本和设置版本。
- 新增模块、操作、字段和设置权限项，继续绑定广州 ERP 用户及角色，不建立账号表。
- 本地 `git diff --check` 通过；迁移、引导和兼容 helpers 经服务器 PHP 8.0 标准输入语法检查通过。
- 数据库实际迁移、回滚演练、旧 BOM 行数/列定义保护在该基础提交部署后执行并继续记录。
