# 2026-08-05：物料中心产品参数功能

- 用户明确产品参数应属于“物料中心”，不是产品适配页面里的临时设置。本次新增中心化页面 `material_center_v1/product_parameters.php`。
- 新增左侧业务菜单“产品参数”，用于集中维护产品功率、电流、电压、色温、显指、光束角、尺寸、安装方式、电源方式、调光方式和备注。
- 新增保存接口 `material_center_v1/api/v1/product-parameters.php`，通过统一登录、物料中心查看权限、编辑权限和 CSRF 校验。
- 参数保存到 `mc_products.snapshot_json.product_parameters`，不新增表、不修改旧 BOM、不回写旧产品适配业务。
- 产品适配 V2 技术范围解析优先读取 `product_parameters`，让后续芯片、电源、光学适配可复用物料中心主数据。
- 本地 `git diff --check` 通过；本机无 PHP，已将候选文件传至服务器 `/tmp` 并用服务器 PHP 完成语法检查。
- 提交 `d797901a42ba5b961d1e3b45f9f599231a0f5392` 已推送 GitHub 并同步正式服务器；正式目录 PHP 语法通过，产品参数页 CLI 渲染无 Fatal。

## 2026-08-05：产品参数弹窗统一为 V2 宽版样式

- 用户要求产品参数维护弹窗按 V2 设置逻辑弹窗样式统一。
- 调整 `material_center_v1/product_parameters.php`：产品参数维护弹窗改为宽版 1280px 上限、顶部渐变标题区、文字“关闭”按钮、绿色虚线提示、四列表单、内部滚动正文和固定底部操作区。
- 表单分区调整为“电气参数 / 光学与外观 / 结构尺寸”，并保留原字段、原保存接口与 `mc_products.snapshot_json.product_parameters` 存储位置。
- 新增 `material_center_v1/tests/product_parameters_modal_contract.php` 锁定弹窗视觉结构与关键字段。
- 本次仅为 UI 统一与回归测试，不修改旧 BOM、不修改旧产品适配业务、不新增数据库表。
- 功能提交 `3edc350ce54a09c55bef5ba4359b841e4bbcfdf4` 已推送并同步正式服务器；正式目录 PHP 语法、合同测试和产品参数页 CLI 渲染通过。

## 2026-08-05：产品参数支持产品类型模板与自定义字段

- 用户补充参数需要覆盖资料表中的 Model、Cut-out Size、Dimensions、Power、Luminous flux、Adjustability、Beam Angle、CCT、CRI、UGR、Dimming method、IP rating、Best for，并且导轨灯、磁吸式、明装式、线性等产品需要不同参数。
- 产品参数弹窗新增“参数模板 / 产品类型”：嵌入式、导轨灯、磁吸式、明装式、线性、自定义。
- 新增规格表参数字段：开孔尺寸、外形尺寸、功率规格、光通量、倾斜角、旋转角、光束角文本、CCT 文本、CRI 文本、UGR、调光方式文本、Best for。
- 新增自定义参数区：字段名称、参数值、单位、分组；支持快速加入嵌入式/导轨灯/磁吸式/明装式/线性常用字段，也支持空白字段。
- 保存接口增加新增字段和 `custom_fields` 数组净化保存，仍写入 `mc_products.snapshot_json.product_parameters`；不新增表、不修改旧 BOM、不回写旧产品适配业务。
- 更新 `material_center_v1/tests/product_parameters_modal_contract.php` 覆盖新增字段、产品类型、自定义字段和服务端保存能力。

# Codex 十阶段连续执行日志

## 第 1 阶段：审计、备份、基线锁定

- 状态：完成。
- 完整阅读 `ARTDON_MATERIAL_CENTER_CODEX_LONG_RUN_MASTER_SPEC_V1.md`（748 行）。
- 锁定 `components/sidebar.php` 为菜单基线，不修改现有菜单。
- 建立本地 Git bundle、服务器目录 tar、全数据库逐表 PDO 备份并完成校验。
- 确认旧 BOM 来源为 `bom_materials`，后续只读。
- 发现外壳与原服务层引导断裂、首页静态数据、业务占位页和旧测试断言四类缺口。
- 修复策略：保留当前外壳，恢复自动加载、统一账号和数据库引导；不恢复旧视觉外壳。

## 阶段记录规则

每阶段记录迁移、服务、接口、页面、权限、测试、旧表只读核对和遗留问题。测试失败必须先修复再进入下一阶段；非阻断问题写入 `KNOWN_ISSUES.md`。

## 第 2 阶段：基础架构与数据库（基础提交）

- 恢复当前外壳缺失的命名空间自动加载、广州 ERP `includes/bootstrap.php`、数据库、Session 和 CSRF 引导。
- 保留现有 `components/` 外壳与左侧菜单；仅让旧服务层和当前外壳能够同时运行。
- 新增可回滚迁移 `20260726_005_master_data_full_domain`，所有新表均使用 `mc_`。
- 覆盖七类扩展、字段注册、审批、操作/修改日志、BOM 来源、导入错误、供应商联系人/阶梯价/MOQ/交期、产品适配、替代引用迁移、文档版本和设置版本。
- 新增模块、操作、字段和设置权限项，继续绑定广州 ERP 用户及角色，不建立账号表。
- 本地 `git diff --check` 通过；迁移、引导和兼容 helpers 经服务器 PHP 8.0 标准输入语法检查通过。
- 数据库实际迁移、回滚演练、旧 BOM 行数/列定义保护在该基础提交部署后执行并继续记录。

### 第 2 阶段服务器验证

- `20260726_005` 已完成 up、down、up 演练。
- 演练前后 `bom_materials` 均为 1022 行，字段签名均为 `88846b7e...fdae7f`。
- MM schema、旧 BOM 只读、设置、权限和旧产品只读合同测试通过。

## 第 3–4 阶段：主库生命周期与七类物料（完成）

- 当前外壳的八个物料入口已移除 `demo/materials.php` 依赖，统一读取 `mc_materials`、分类、元数据和电源扩展真实数据。
- 新建弹窗已接入统一 CSRF、统一账号权限及 `material-master.php`，保存真实草稿，不再显示静态保存成功。
- 新建和编辑改为事务写入；同步创建 `mc_material_metadata`，并写活动日志和统一操作日志。
- 引用检查扩展到供应商、替代、产品适配、旧来源和电源规则。
- 新增七类分类及动态字段注册种子迁移 `20260726_006_categories_and_fields`，带回滚。
- 当前修改经 PHP 8.0 和 JavaScript 语法检查、`git diff --check` 通过；部署后继续执行迁移和真实事务回滚测试。

### 第 3–4 阶段服务器验证

- `20260726_006` 已应用。
- 新建芯片草稿、元数据和日志在测试事务内成功写入并完整回滚，测试记录未保留。
- 八个物料入口 CLI 渲染通过，旧 BOM 仍为 1022 行。

## 第 5 阶段：BOM 只读接入（基础同步）

- `LegacyBomMaterialAdapter` 增加只允许 SELECT 的分批读取，单批最大 1000。
- 新增 `SourceSyncService`：按旧 BOM 主键保存来源快照和 SHA-256，重复运行幂等；变化记录标记为 `changed`，不静默覆盖正式物料。
- 每 500 条独立事务提交，只写 `mc_source_records` 和 `mc_operation_logs`。
- 数据接入页已移除静态任务和静态状态，展示真实旧 BOM、来源快照、待整理、变化、导入错误和任务数据。
- 新增带统一账号、权限和 CSRF 校验的同步 API；前端明确反馈失败，不伪造成功。
- PHP 8.0、JavaScript 语法和 `git diff --check` 通过；部署后执行两次幂等同步及旧表保护核对。

### 第 5 阶段同步验证与文件导入

- 首次 BOM 快照同步：读取 1022、新建 1022；第二次读取 1022、新建 0、变化 0、不变 1022，幂等通过。
- 同步前后旧 BOM 均为 1022 行，结构签名均为 `88846b7e...fdae7f`。
- 新增 CSV/XLSX 上传、10MB 限制、字段规范化、逐行预检、错误明细、任务记录和确认执行。
- 预检有错误时禁止执行；全部通过后在单一事务中创建真实草稿，任一行失败整体回滚。
- 文件和任务均使用随机 UUID；文件写入 `storage/imports`，不写旧系统上传目录。

## 第 6 阶段：供应商、价格、MOQ、交期

- 新增可回滚 `mc_supplier_profiles`、`mc_supplier_attachments`。
- `SupplierService` 提供真实供应商、联系人、供应商物料、最新价、价格历史、阶梯价、MOQ 和交期读写。
- 供应商和价格保存均为事务；每次价格保存追加历史，不覆盖历史价格。
- 服务端再次校验供应商、物料、非负价格、三位币种，并写统一操作日志。
- 供应商页面移除 18 家静态供应商和静态价格，改为真实数据库空状态、列表、详情及新增表单。
- API 复用统一账号、`material_center.supplier.manage` 和 CSRF。
- PHP 8.0、JavaScript 语法和差异检查通过；部署后执行迁移回滚及不保留数据的事务测试。

### 第 5–6 阶段服务器验证

- `20260726_007` 完成 down→up。
- 真实供应商、联系人、供应商物料、价格、价格历史、两档阶梯价、MOQ、交期保存通过。
- CSV 上传、逐行预检及执行创建草稿通过；测试任务、文件、物料、供应商和价格全部清理。
- 测试前后旧 BOM 均为 1022 行。

## 第 7 阶段：产品适配

- 命名系统产品通过 `LegacyProductAdapter` 只读分批读取，保存 `mc_products` 快照及哈希；重复同步幂等。
- 新增通用适配服务：配置组、物料选项、默认、类型、价格影响、交期影响、条件、批准及批准结果读取。
- 产品批准前验证每个配置组至少有一个真实物料选项。
- 新增商务中心可使用的批准结果只读 API；未批准草稿不会输出。
- 产品适配页移除五条静态产品、七组静态规则和三条静态物料，改为真实产品、组、选项和空状态。
- 写接口复用统一账号、服务端适配权限和 CSRF；审批及变更写 `mc_adaptation_logs`。
- PHP 8.0、JavaScript 和差异检查通过；部署后执行全产品幂等同步及不保留规则数据的事务测试。

### 第 7 阶段服务器验证

- 命名系统只读同步 243 个产品；第二次同步新增 0、变化 0。
- 真实配置组、物料选项、默认、条件、价格/交期影响、批准及批准结果读取通过。
- 测试规则和物料全部清理；旧 `naming_models` 保持 243 行。

## 第 8 阶段：替代、版本和引用迁移

- 新增 `SubstitutionService`，支持直接、条件、临时、停产替代、优先级和生效时间。
- 保存和批准均执行图遍历，任何可达回路都会拒绝，防止直接和多级死循环。
- 每次替代保存、批准、引用迁移和回滚都会生成物料版本快照。
- 影响分析复用统一引用检查，覆盖旧来源链接、供应商、产品适配、电源规则和既有替代。
- 引用迁移仅处理 `mc_` 表，绝不更新旧 BOM；迁移记录精确保存各表行 ID。
- 回滚只恢复该批次实际迁移的行，不会误改目标物料原有引用。
- 替代页面已移除三条静态关系，展示真实替代数据和空状态。
- PHP 8.0 与差异检查通过；部署后测试循环拒绝、迁移、精确回滚、版本及数据清理。

### 第 8 阶段服务器验证

- A→B→C 后创建 C→A 被服务端拒绝。
- 适配引用从 A 迁移至 B 后，按迁移批次精确回滚至 A。
- 替代保存、迁移和回滚生成物料版本；测试数据清理完成。
- 第一次测试清理脚本因 MySQL 引号不兼容失败；已修复并单独清除 3 条 `SUB-*` 测试物料，复核残留 0。

## 第 9 阶段：文档、日志、设置和权限

- 新增物料文档上传、25MB 限制、扩展名白名单、内容类型检测、随机存储名、0640 权限、SHA-256、访问级别和版本。
- 文档下载必须经过统一登录和服务端查看权限；上传、删除必须经过文档维护权限及 CSRF。
- 文档页移除静态文件，展示真实文件版本和真实操作日志。
- 设置页接入现有 `SettingsService` 白名单校验；支持个人设置保存、JSON 导入导出、恢复默认。
- 每次设置保存或恢复生成 `mc_setting_versions` 快照。
- 新增权限管理服务与 API，支持旧 ERP 用户 ID / 角色键、允许/拒绝、数据范围 JSON 和权限审计；无权限用户不会看到授权数据。
- 采购价等敏感字段继续通过服务端字段权限控制，不只隐藏按钮。
- PHP 8.0、JavaScript 和差异检查通过；部署后测试文件版本、恶意扩展拒绝、设置版本、非法设置拒绝、授权审计及清理。

### 第 9 阶段服务器验证

- 同一文档的两个 PDF 版本、SHA-256 和版本号保存通过，测试文件与记录已清理。
- PHP 伪装上传被内容签名校验拒绝。
- 非法设置值被白名单拒绝，合法设置生成版本快照；权限授予和审计写入通过，测试数据已清理。

## 第 10 阶段：全站测试、安全、性能与交付（完成）

- 工作台待办、适配冲突、价格变化和最近活动已改为读取 `mc_` 真实数据；无数据时明确显示零或空状态。
- 顶栏身份改为广州 ERP 当前会话账号和角色，不再硬编码演示用户。
- 旧类别工作台和旧产品适配 URL 改为兼容跳转到现行真实页面。
- 删除已无引用的 `demo/materials.php`，现行业务页面不再保留演示物料源。
- 物料查询层增加参数绑定的 count/分页能力；当前页记录不超过服务端上限。
- 新增带登录和导出权限校验的真实物料 CSV 导出，处理 UTF-8 BOM 和公式注入前缀。
- 本地全部 JavaScript 合同/语法测试通过；服务器全部 PHP 测试、全量 PHP 语法、迁移和数据库查询通过。
- 线上首页 HTTP 200，旧类别和产品适配地址正确 302 到现行页面。
- 自动化环境无可连接浏览器实例，多分辨率登录态视觉截图列入 `KNOWN_ISSUES.md`，未伪报通过。

### 严格收尾审计

- 移除旧电源工作台所有 `data-ui-not-connected` 正式动作，改为现行业务入口并增加反占位合同测试。
- 物料导入增加带权限的错误 CSV 下载，输出做公式注入保护。
- 完成供应商价格 CSV/XLSX 批量预检、错误记录、任务并发占用、确认执行和逐条待审批；服务器真实集成测试通过。
- 新增迁移 `20260726_012_supplier_documents`，完成供应商附件的内容签名、随机存储、SHA-256、权限、预览和下载；完成 up/down/up 与两次功能验证。
- 物料文档增加 PDF/图片 inline 预览、CSP、nosniff 和访问级别的服务端权限校验。
- 保留旧导航结构和标签，仅将旧兼容外壳可达的占位地址映射到现行真实页面。
- 七类动态字段、角色矩阵、并发编辑、最终业务验收、全量 PHP/JS/路由/静态测试再次执行。

### 阶段完成结论

- 十个阶段均已完成；所有数据库变化有迁移及回滚。
- 本地、GitHub `origin/main` 和服务器运行目录使用同一提交。
- 旧 `bom_materials` 保持 1022 行且结构签名不变；测试残留为 0。
- 唯一未自动完成项是登录态真实浏览器多内核截图，已作为非阻断限制如实记录，没有用静态数据或假成功代替。
# 2026-07-26：电源统一编辑抽屉

- 纠正电源列表原“查看详情”中的假表单：旧表单只由前端拼接，安装、功率档、质保等控件没有绑定真实保存。
- 新增 `PowerEditorService` 与 `api/v1/power-editor.php`，统一处理新建电源、草稿详情、单条保存、批量预览、批量执行和批次回滚。
- 单条抽屉按基本资料、功率与输入、输出、安装与尺寸、调光与认证、采购与供应商质保分区；多输出电流和多调光分别写入正式关联表。
- 批量抽屉采用逐项启用的设置卡，不使用弹窗表格；支持只填空值、覆盖已有值、影响预览、跳过数量、事务执行和批次回滚。
- 电源页移除“更多”下拉；导入、导出、日志改为明确入口。新建电源也使用同一个分区抽屉，不再打开旧通用弹窗。
- 旧 BOM 来源保持只读；点击来源行可在统一抽屉查看，整理动作继续进入正式标准化流程，不写旧表。
