# Settings Model

设置优先级为系统默认→全局→角色→用户，数据位于 `mc_ui_settings`、`mc_ui_setting_scopes`、主题/版本、角色/用户偏好和审计表。所有值经白名单校验后映射为设计令牌，禁止任意 CSS。A7 全区域排版、主题导入导出和回滚界面尚待继续。

A7 已覆盖正文、导航、顶栏、标题、副标题、表格、标签、输入、按钮、弹窗/抽屉标题和分页字号，以及页面/卡片/边框/文字/状态色、布局尺寸、表格行高、分页模式、动画和展示模式。JSON 导入不会自动保存。
