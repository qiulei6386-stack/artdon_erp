# 电源领域模型（只读发现阶段）

当前电源列表不是正式迁移模型，仅从旧 `bom_materials` 中识别名称或分类包含“电源/驱动”的有效记录。

可显示字段与物料只读视图一致：

- 源 ID、分类、名称、品牌、型号、规格、单位、材料牌号、更新时间。

当前明确不提供：

- 电源新增、编辑或删除。
- 电气参数结构化编辑。
- 正式迁移状态。
- 供应商、价格或成本。
- 对旧 BOM 表的任何写入。
