# 回滚计划

代码按 Git 提交回退；数据库按迁移版本调用 `MigrationRunner::rollback()`。回滚 F3/F4/F9 时先停止模块写入，导出 `mc_` 新表备份，再按迁移 `down` 逆序删除新增表。旧 BOM、旧产品及旧账号表不参与回滚，因为本轮从未修改。

不得在未备份正式 `mc_` 数据时执行生产回滚。回滚后重新运行 PHP 语法、健康接口和旧表指纹检查。
