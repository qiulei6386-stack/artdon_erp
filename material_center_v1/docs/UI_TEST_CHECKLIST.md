# UI 测试清单

- [x] PHP 语法检查
- [x] JavaScript 语法检查
- [x] UI 文件均位于 `material_center_v1`
- [x] 首页引用统一组件入口
- [x] 浅色、深色、跟随系统主题
- [x] 下拉菜单外部点击与 Esc 关闭
- [x] 弹窗/抽屉遮罩、Esc、焦点返回和滚动锁定
- [x] 表格键盘 Enter/Space 打开详情
- [x] 表格超过 20 条自动分页
- [x] 表格排序、选择、全选与半选
- [x] 列宽拖动与双击自动适配
- [x] 列显示隐藏与三档行密度
- [x] 操作列固定与表格内部滚动
- [x] 搜索清空与 300ms 自动提交
- [x] 空、未登录、数据不可用状态
- [x] 900px 与 560px 响应式断点
- [x] `prefers-reduced-motion`
- [x] Modal S/M/L 尺寸与 ConfirmModal
- [x] 菜单防越界、最大高度与内部滚动
- [x] 侧栏折叠记忆与小屏导航抽屉
- [x] 电源列表、BOM 审计和系统状态使用统一组件
- [ ] 登录态真实物料数据人工业务验收

## 2026-07-25 自动检查结果

- `node tests/ui_static_test.js`：通过（16 个必需文件/页面）。
- `node tests/ui_contract_test.js`：通过（统一组件契约及只读安全）。
- `node --check`：模块全部 JavaScript 通过。
- `git diff --check`：通过。
- 服务器 `php -l`：模块全部 12 个 PHP 文件通过。
- 线上 HTTP：首页、电源列表、BOM 审计、系统状态、组件展示、健康接口均为 200。
- 1440/1728/1920：布局使用无整页横向滚动的弹性容器，表格横向滚动限制在 `.ui-table-wrap`；仍建议登录态人工截图复核。
- 小屏：900px 以下侧栏切换为可关闭抽屉；560px 以下工具区单列；仍建议真实手机人工触控验收。
- 保存成功：组件展厅真实执行加载、状态恢复、成功 Toast 和关闭。
- 保存失败/网络失败：统一错误 Toast 状态已展示；本轮没有获准的真实写接口，未伪造后端保存失败。
