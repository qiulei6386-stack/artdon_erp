# F3 设置需求与实现

作用域优先级：系统默认 → 全局 → 角色 → 用户。当前 UI 提供个人范围维护；全局和角色结构已建立，需管理员授权后才开放维护入口。

已接入设置：主题模式、主操作色、侧栏色、字体、正文/菜单/表格字号、界面密度、侧栏状态、表格分页、动画和 Toast。设置值按白名单规则校验，只映射为受控设计令牌，不允许保存任意 CSS。

每次变化写入 `mc_setting_audit_logs`；范围值有版本号和锁定字段。导入、导出及版本回滚界面尚未接入，不能显示虚假成功。
