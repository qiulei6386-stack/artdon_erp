# 已知限制

- 物料中心不会修复旧 BOM 或命名系统的历史脏数据；只保存来源快照并进入待确认流程。
- CSV/XLSX、文档和批量上限用于保护线上资源，超限任务需拆分。
- 旧兼容 URL 继续保留并跳转/映射现行页面，旧书签仍可使用。
- 当前自动化环境没有可连接的登录态浏览器实例，因此 Chrome / Edge / Safari 在 1920×1080、1440×900、1366×768 和移动端的最终视觉截图需人工补验。已完成服务端渲染、HTTP、静态 UI 合同、响应式 CSS 和交互脚本检查；此限制不影响数据库或业务闭环，但不计作真实浏览器视觉通过。
