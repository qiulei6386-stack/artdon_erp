# 禁止触碰范围

本模块开发不得：

1. 修改、替换、删除或重命名 `material_center_v1` 外的旧业务文件。
2. 修改 `commercial_center_v1`。
3. 修改旧数据库表结构或写入 `bom_materials`。
4. 建立独立账号、权限或登录系统。
5. 把本模块 UI、CSS、JavaScript、组件或测试散落到 ERP 根目录。
6. 将未接入功能显示为成功。

本文件仅记录本轮明确安全边界，不推断旧系统未提供的规则。
