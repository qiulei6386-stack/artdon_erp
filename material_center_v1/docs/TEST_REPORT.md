# 总规范测试报告

测试日期：2026-07-26（Asia/Shanghai）

## 数据与迁移

- 迁移 `001–012` 已应用；关键迁移执行 down→up，`012` 执行“up→功能测试→down→up→功能测试”。
- 旧 BOM 两次同步：读取 1022；第二次新增 0、变化 0，结构签名保持 `88846b7e...fdae7f`。
- 命名产品同步 243 条，第二次新增/变化均为 0。
- 七类字段覆盖：电源 21、芯片 17、光学 11、型材 12、接头 11、配件 9、包装 11；合法写入/复制和非法范围拒绝通过。
- 10,000 条分页查询约 0.020 秒；测试数据全部清理。

## 业务

- 新增、编辑、复制、乐观锁冲突、状态流转、引用检查、批量和权限过滤导出通过。
- 物料 CSV/XLSX 预检、错误阻断、错误 CSV 和事务执行通过。
- 供应商资料、价格历史、阶梯价、MOQ、交期、首选供应商、价格审批、价格批量导入通过。
- 产品配置组、选项、默认、条件、冲突、明确不适配原因、价格/交期影响和审批结果通过。
- 四类替代、多级循环拒绝、影响分析、引用迁移及精确回滚通过。
- 物料文档版本/签名/哈希/恶意伪装拒绝/预览权限，以及供应商附件迁移和上传读取通过。
- 设置白名单、导入导出、版本、恢复默认、权限授权和审计通过。

## 安全、权限与静态检查

- 六类角色矩阵、显式 deny、字段隐藏/掩码、数据范围通过。
- 未登录写 API 返回 JSON 401；CSRF、SQL 参数绑定、XSS 转义、CSV 公式注入、路径穿越、大文件和文件签名检查通过。
- 全量 PHP 语法、全部 PHP 合同/集成测试、全部 JavaScript 语法及 UI 静态合同通过。
- 正式导航无无效动作；兼容 URL 保留并映射现行真实页面。
- 浏览器登录态视觉自动化限制见 `KNOWN_ISSUES.md`，未伪报视觉截图通过。
# 2026-07-26：电源统一编辑专项

- JavaScript 语法：`assets/js/power-editor.js` 通过 `node --check`。
- PHP 语法：服务、API、布局、契约测试和集成测试均通过服务器 PHP 解释器标准输入只读检查。
- 静态回归：UI static、UI contract/read-only safety 通过。
- 契约测试：统一分区抽屉、非表格式批量设置、明确工具栏入口、权限/CSRF/事务/回滚标记通过。
- 集成测试覆盖：新建、单条保存、多输出电流、多调光、批量预览、批量执行、批次回滚及测试数据清理；部署后在正式 `mc_*` 数据库执行。
- 首次服务器集成测试发现批量查询中的 `p.*` 覆盖了同名 `m.id`，导致批量目标使用电源规格行 ID；测试正确失败并完成数据清理。已改为唯一 `target_material_id`，修复后重新执行完整闭环。
- 浏览器限制：自动浏览器运行时返回可用浏览器列表为空，未伪报登录态点击或截图通过。
