(()=>{document.querySelectorAll('[data-adaptation-action]').forEach(form=>form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{const response=await fetch(`${window.MC_BASE_URL}/api/v1/adaptation.php`,{method:'POST',body:new FormData(form),credentials:'same-origin',headers:{Accept:'application/json'}});const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'操作失败');location.reload();}catch(error){alert(error instanceof Error?error.message:'操作失败');button.disabled=false;}}));})();
