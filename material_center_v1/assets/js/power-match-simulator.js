document.addEventListener('DOMContentLoaded',()=>document.querySelector('#simulation-form')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,button=form.querySelector('button');button.disabled=true;button.setAttribute('aria-busy','true');const body=new FormData(form);body.set('action','simulate');try{const response=await fetch('api/v1/product-power-rules.php',{method:'POST',body,credentials:'same-origin'}),data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'模拟失败');const result=data.data,root=document.querySelector('#simulation-result');root.innerHTML=result.matches.length?`<div class="ui-table-wrap"><table class="ui-table"><thead><tr><th>物料</th><th>品牌/型号</th><th>结果</th><th>原因</th></tr></thead><tbody>${result.matches.map(x=>`<tr><td>${escapeHtml(x.material_code)}</td><td>${escapeHtml([x.brand,x.model].filter(Boolean).join(' / '))}</td><td><span class="ui-badge">${x.result==='auto_match'?'自动匹配':'不匹配'}</span></td><td>${escapeHtml(x.reasons.join('；'))}</td></tr>`).join('')}</tbody></table></div>`:'<div class="ui-state"><div class="ui-state-inner"><h2>暂无正式电源可匹配</h2><p>没有伪造结果；请先完成人工确认并建立正式电源。</p></div></div>';window.ArtdonUI.toast(`模拟完成：${result.summary.auto_match}/${result.summary.total} 自动匹配`,'success');}catch(error){window.ArtdonUI.toast(error.message||'网络失败','error');}finally{button.disabled=false;button.removeAttribute('aria-busy');}}));function escapeHtml(v){const d=document.createElement('div');d.textContent=v??'';return d.innerHTML;}
