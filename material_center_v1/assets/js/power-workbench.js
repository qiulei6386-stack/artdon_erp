document.addEventListener('DOMContentLoaded',()=>{
 document.querySelector('[data-reset-power-view]')?.addEventListener('click',()=>{Object.keys(localStorage).filter(k=>k.startsWith('mc-table:')&&k.includes('power_workbench')).forEach(k=>localStorage.removeItem(k));window.ArtdonUI.toast('电源表格视图已恢复默认','success');setTimeout(()=>location.reload(),300);});
 document.querySelector('[data-export-power]')?.addEventListener('click',button=>{const table=document.querySelector('#power-workbench-table');if(!table)return window.ArtdonUI.toast('当前没有可导出的真实数据','warning');const lines=[...table.rows].filter(r=>!r.hidden).map(row=>[...row.cells].slice(1,-1).map(cell=>`"${cell.textContent.trim().replaceAll('"','""')}"`).join(','));const blob=new Blob(['\ufeff'+lines.join('\n')],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`power-${button.currentTarget.dataset.tab}.csv`;a.click();URL.revokeObjectURL(a.href);window.ArtdonUI.toast('当前可见结果已导出','success');});
});
