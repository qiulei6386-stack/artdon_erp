(()=>{const form=document.querySelector('[data-supplier-form]');form?.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('[type="submit"],button:not([type])');button.disabled=true;try{const response=await fetch(`${window.MC_BASE_URL}/api/v1/suppliers.php`,{method:'POST',body:new FormData(form),credentials:'same-origin',headers:{Accept:'application/json'}});const data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'保存失败');location.href=`?supplier_id=${data.data.id}`;}catch(error){alert(error instanceof Error?error.message:'保存失败');button.disabled=false;}});})();
